import { SITE } from "../Constants";

const Hero = () => {
  return (
    <section className=" ">
      <div className="flex justify-center font-orbitron text-3xl mt-5 py-3 px-4">
        <h1>{SITE.description}</h1>
      </div>

      
     
    </section>
  );
};

export default Hero;
